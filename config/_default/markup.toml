# -- Markup --
# These settings are required for the theme to function.

[goldmark]
[goldmark.renderer]
  unsafe = true

[highlight]
  noClasses = false
  lineNos = true  # Enable line numbers in code blocks

[tableOfContents]
  startLevel = 2
  endLevel = 4
